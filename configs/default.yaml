---
seed: 42

labels:
  - Center
  - Donut
  - Edge-Loc
  - Edge-Ring
  - Loc
  - Near-full
  - Random
  - Scratch

paths:
  data_dir: data
  bronze_dir: data/bronze
  silver_dir: data/silver
  gold_dir: data/gold
  models_dir: models
  figures_dir: docs/figures
  mlruns_dir: mlruns

training:
  batch_size: 128
  lr: 0.001
  epochs: 30
  early_stopping_patience: 7

mlflow:
  tracking_uri: auto
  experiment_name: wm811k-defect
